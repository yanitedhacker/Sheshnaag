[pytest]
addopts = -q
testpaths = tests
markers =
  unit: fast unit tests
  integration: integration tests (docker lab)
  red_team: adversary / escape harness tests
  kvm: requires KVM/libvirt host
  load: load/stress tests (not run by default)
